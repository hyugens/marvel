import { TruncateDescPipe } from './truncate-desc.pipe';

describe('TruncateDescPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateDescPipe();
    expect(pipe).toBeTruthy();
  });
});
